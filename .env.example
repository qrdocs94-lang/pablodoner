# ============================================================
# Pablo Döner Terminal — Environment Variables
# Copy this file to .env.local and fill in your values
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
# Found at: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://DEIN-PROJEKT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...

# ── App URL (used for Stripe redirect) ───────────────────────
APP_URL=http://localhost:3000

# ── Supabase Edge Function Secrets (set via CLI) ─────────────
# supabase secrets set STRIPE_SECRET_KEY=sk_live_...
# supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
# supabase secrets set APP_URL=https://pablo-doener.de

# ── Stripe (for local dev only — use secrets for production) ─
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
